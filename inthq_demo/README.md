# IntHQ Demo

This folder contains the public S/I/F implementation of IntHQ. Scenario/context features are encoded on S tokens, POI intention features remain on I tokens, and feedback features remain on F tokens. The model uses a 16-layer ordinary-residual dual-stream encoder with task interaction, hierarchical querying, and DSFNet.

```bash
bash inthq_demo/run.sh
```
