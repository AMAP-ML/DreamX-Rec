# Shared dependencies for the CV, NLP, and recommendation tasks.
# Versions are pinned to a verified working environment.
numpy==1.26.4
torch==2.6.0
torchvision==0.21.0
timm==1.0.28
datasets==2.14.6
pyarrow==14.0.0
transformers==4.45.2
tokenizers==0.20.3
accelerate==1.13.0

# NLP additionally requires FlashAttention 2 because the LLaMA configuration
# uses attn_implementation="flash_attention_2". Install it after this file so
# the build can reuse the installed PyTorch instead of an isolated build env:
#
#   pip install flash-attn==2.5.6 --no-build-isolation
#
# Building flash-attn requires nvcc and can take several minutes.

# The optional recommendation --backend recis path uses RecIS dynamic hash
# tables. RecIS has to be built separately; it is not installed by this file.
# Release: https://github.com/alibaba/RecIS/releases/tag/v1.2.0
# Installation: https://alibaba.github.io/RecIS/installation.html
#
#   git clone https://github.com/alibaba/RecIS.git
#   cd RecIS
#   git checkout v1.2.0
#   git submodule update --init --recursive
#   bash build.sh 0
#   pip install "$(find ./dist -maxdepth 1 -name 'recis*.whl' -print -quit)"
#
# RecIS v1.2.0 requires Python 3.10+, PyTorch 2.4+, and CUDA 12.4+
# (or ROCm 7.0+).
